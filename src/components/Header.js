import React from 'react';
import '../stylesheets/Header.css';

const Header = () => (
  <header>
    <div className="title">
      <h4>TrackYourReading</h4>
    </div>
  </header>
);

export default (Header);
