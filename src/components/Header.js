import React from 'react';
import '../stylesheets/Header.css';

const Header = () => (
  <header>
    <div className="title">
      <p>Reading-Tracker</p>
    </div>
  </header>
);

export default (Header);
